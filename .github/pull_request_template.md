<!--
PR 门禁清单 —— 依据《第一版全局软硬件接口标准》V1.0 §8.3
提交前请逐项确认。任何一项不满足，PR 不予合并。
-->

## 变更摘要

<!-- 一两句话说清这个 PR 做了什么 -->

## 变更原因与影响范围

- **原因**：
- **受影响的大组**：
- **受影响接口**：

## 兼容性

- [ ] 不改变任何已有字段含义（V1.x 允许）
- [ ] 若为破坏性修改，已附 ICR 编号：`ICR #___`，且已获六大组接口负责人共同确认（标准 §8.4 第 3 步）
- [ ] 接口变更已同步更新 `docs/interfaces/interface_registry.yaml`（标准 §8.3）

## 验证

**验证命令：**

```bash
cd wheelchair_ws
colcon build --symlink-install
colcon test
ros2 interface list | grep wheelchair_interfaces
ros2 topic list && ros2 topic type /wheelchair/safety/status
ros2 run tf2_tools view_frames        # IF-04 TF 单树连通
```

**测试结果：**

<!-- 粘贴构建日志摘要与测试通过情况。接口包必须能独立构建（IF-01）。 -->

## 评审（标准 §8.3 第 1 条：至少一名代码所有者和一名受影响大组接口负责人）

- 代码所有者：
- 受影响大组接口负责人：

## 回退方法

<!-- 如何安全回退这次变更 -->

## AI 工具使用记录（标准 §10）

| 工具名称 | 输入目的 | 采用内容 | 人工修改 | 验证结果 |
|---|---|---|---|---|
|  |  |  |  |  |

<!-- 涉及速度、急停、推杆、姿态保护、路径规划或机械臂运动的 AI 建议，
     必须通过仿真、单元测试和安全工况测试后方可使用。无则填"无"。 -->

## 自查

- [ ] 未提交 `build/`、`install/`、`log/`
- [ ] 未提交模型大文件、录制数据
- [ ] 未提交 API 密钥、密码、个人绝对路径（标准 §8.3）
